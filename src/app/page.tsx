export const dynamic = "force-static"


export default function Page() {
  return (
    <section className="space-y-4">
      <h1 className="text-2xl font-semibold">Test Task Frontend NextJS</h1>
      <p>
        Navigation: CSR / SSR / SSG / ISR.
        Button «Open form» — upload dialog with POST.
      </p>
    </section>
  )
}